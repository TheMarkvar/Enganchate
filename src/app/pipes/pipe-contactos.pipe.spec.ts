import { PipeContactosPipe } from './pipe-contactos.pipe';

describe('PipeContactosPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeContactosPipe();
    expect(pipe).toBeTruthy();
  });
});
