import { PipeCategoriasPipe } from './pipe-categorias.pipe';

describe('PipeCategoriasPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeCategoriasPipe();
    expect(pipe).toBeTruthy();
  });
});
